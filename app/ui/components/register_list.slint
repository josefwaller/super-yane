import { Register } from "./register.slint";
import { ScrollView } from "std-widgets.slint";

// Renders a list of Registers
export component RegisterList inherits ScrollView {
    height: 100%;
    width: 100%;

    VerticalLayout {
        alignment: start;
        spacing: 5px;
        padding: 10px;
        vertical-stretch: 0;

        @children
    }
}
