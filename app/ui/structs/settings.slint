import { StandardController } from "./standard_controller.slint";
import {
    BinaryDataSource,
} from "../components/binary_data.slint";

// Settings for how to run the emulator.
// Sent from slint to the emulation thread.
export struct Settings { volume: float, is_paused: bool, binary_source: BinaryDataSource}
