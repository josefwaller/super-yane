// Data from the PPU to display
export struct PpuData {
    vblank: int,
    forced_blanking: int,
    brightness: int,
    bg_mode: int,
    bg3_prio: int,
    mosaic_size: int,
    vram_increment_mode: string,
    vram_increment_amount: int,
    vram_addr: int,
    cgram_addr: int,
}
