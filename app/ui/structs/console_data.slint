import { CpuData } from "./cpu_data.slint";
import { PpuData } from "./ppu_data.slint";
// Data from the console
export struct ConsoleData {
    cpu: CpuData,
    ppu: PpuData,
    binary-data: [[string]],
}
